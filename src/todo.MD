

optional {
    selection screen {
        flips rotate 90 degs each time instead of just being 0 or 90.
        random button to not press go but instead allows multiple clicks
    }
    full game {
        sound effects
        potential refactor {
        look through the code and see if it can be made cleaner.  I did not keep many coding principals in mind when writing this unfortunately.
    }
    }
}

